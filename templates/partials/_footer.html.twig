<footer class="bg-darkGrey border-t border-neonBlue text-white mt-12">
    <div class="max-w-7xl mx-auto px-6 py-6 flex flex-col sm:flex-row justify-between items-center gap-4 sm:gap-0">
        
        {# Copyright #}
        <p class="text-sm text-gray-300 text-center sm:text-left">
            &copy; {{ "now"|date("Y") }} Lannify. Tous droits réservés.
        </p>
        
        {# Liens légaux #}
        <nav class="flex flex-col sm:flex-row gap-2 sm:gap-4" aria-label="Liens légaux">
            <a href="{{ path('legal_conditions') }}"
               class="text-sm text-neonBlue hover:text-cyanShadow transition-colors focus:outline-none focus:ring-2 focus:ring-cyanShadow rounded">
                Conditions d’utilisation
            </a>
            <a href="{{ path('legal_privacy') }}"
               class="text-sm text-neonBlue hover:text-cyanShadow transition-colors focus:outline-none focus:ring-2 focus:ring-cyanShadow rounded">
                Politique de confidentialité
            </a>
        </nav>
    </div>
</footer>
