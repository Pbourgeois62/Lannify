<div id="delete-need-{{ need.id }}"
     role="dialog"
     aria-modal="true"
     aria-labelledby="delete-need-title-{{ need.id }}"
     aria-hidden="true"
     class="hidden fixed inset-0 z-[9999] flex justify-center items-center
            bg-black/70 backdrop-blur-sm p-4 overflow-y-auto">

    <div class="relative w-full max-w-sm">
        <div class="glass-modal relative text-center p-6">
            
            <h3 id="delete-need-title-{{ need.id }}"
                class="text-xl font-bold text-white mb-3">
                Supprimer ce besoin ?
            </h3>

            <p class="text-gray-300 mb-6 text-sm">
                Cette action est <span class="text-neonRed font-semibold">irréversible</span>.
            </p>

            <div class="flex justify-center gap-4">
                <a href="{{ path('need_delete', {need: need.id, event: event.id}) }}"
                   class="btn-delete-lg">
                    Oui, supprimer
                </a>

                <button type="button"
                        data-modal-hide="delete-need-{{ need.id }}"
                        class="btn-return">
                    Annuler
                </button>
            </div>
        </div>
    </div>
</div>
