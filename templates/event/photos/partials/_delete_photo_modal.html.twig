<div id="delete-photo-{{ photo.id }}"
     role="dialog"
     aria-modal="true"
     aria-labelledby="delete-photo-title-{{ photo.id }}"
     aria-hidden="true"
     class="hidden fixed inset-0 z-[9999] flex justify-center items-center
            bg-black/70 backdrop-blur-sm p-4 overflow-y-auto">

    <div class="relative w-full max-w-sm">
        <div class="bg-darkGrey border border-neonRed rounded-xl shadow-2xl p-6 text-center">
            
            <h3 id="delete-photo-title-{{ photo.id }}"
                class="text-xl font-bold text-white mb-3">
                Supprimer cette photo ?
            </h3>

            <p class="text-gray-300 mb-6 text-sm">
                Cette action est <span class="text-neonRed font-semibold">irréversible</span>.
            </p>

            <div class="flex justify-center gap-4">
                <a href="{{ path('event_photo_delete', {photo: photo.id, event: event.id}) }}"
                   class="btn-delete">
                    Oui, supprimer
                </a>

                <button type="button"
                        data-modal-hide="delete-photo-{{ photo.id }}"
                        class="btn-return">
                    Annuler
                </button>
            </div>
        </div>
    </div>
</div>
