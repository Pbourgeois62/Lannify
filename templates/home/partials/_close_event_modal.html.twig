{# Modal de confirmation pour clore l'événement #}
<div id="close-event-{{ event.id }}" role="dialog" aria-modal="true" aria-labelledby="close-event-title-{{ event.id }}" aria-hidden="true" class="hidden fixed inset-0 z-[9999] flex justify-center items-center bg-black/70 backdrop-blur-sm p-4 overflow-y-auto">

	<div class="relative w-full max-w-sm">
		<div class="glass-modal-warning">

			<h3 id="close-event-title-{{ event.id }}" class="text-xl font-bold text-white mb-3">
				Souhaitez-vous clore cet événement ?
			</h3>

			<p class="text-gray-300 mb-6 text-sm">
				En faisant cela, vous perdrez l'accès à la gestion de cet événement.
			</p>

			<div class="flex justify-center gap-4">
				<a href="{{ path('event_close', { event: event.id }) }}" class="btn-primary">
					Valider
				</a>

				<button type="button" data-modal-hide="close-event-{{ event.id }}" class="btn-return">
					Annuler
				</button>
			</div>
		</div>
	</div>
</div>
