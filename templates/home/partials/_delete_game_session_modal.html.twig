{# Partial modale suppression événement #}
<div id="delete-game-session-{{ gameSession.id }}"
     role="dialog"
     aria-modal="true"
     aria-labelledby="delete-game-session-title-{{ gameSession.id }}"
     aria-hidden="true"
     class="hidden fixed inset-0 z-[9999] flex justify-center items-center
            bg-black/70 backdrop-blur-sm p-4 overflow-y-auto">

    <div class="relative w-full max-w-sm">
        <div class="glass-modal-danger relative text-center p-6">

            <h3 id="delete-game-session-title-{{ gameSession.id }}"
                class="text-xl font-bold text-white mb-3 drop-shadow-[0_0_6px_#f87171]">
                Supprimer cette session de jeu ?
            </h3>

            <p class="text-gray-200 mb-6 text-sm">
                Cette action est <span class="text-neonRed font-semibold">irréversible</span>.
            </p>

            <div class="flex justify-center gap-4">
                <a href="{{ path('game_session_delete', { gameSession: gameSession.id }) }}"
                   class="btn-delete-lg">
                    Oui, supprimer
                </a>

                <button type="button"
                        data-modal-hide="delete-game-session-{{ gameSession.id }}"
                        class="btn-return">
                    Annuler
                </button>
            </div>
        </div>
    </div>
</div>
